{
  "version": 1,
  "tables": [
    {
      "table": "t_factory_config_pipeline",
      "columns": {
        "name": {
          "type": "string",
          "nullable": false,
          "description": "名称,也是主键,唯一"
        },
        "data_source_key": {
          "type": "string",
          "nullable": false,
          "description": "数据源名称, 引用 t_factory_config_source.name."
        },
        "kind": {
          "type": "string",
          "nullable": false,
          "description": "实现类型, 允许值: builtin, custom. 即内置和自定义"
        },
        "pipeline_code_key": {
          "type": "string",
          "nullable": false,
          "description": "Lakectd 注册的数据加工 pipelineKey。管道只负责向 data_asset_name 指向的既有资产送数，不创建或修改资产定义。"
        },
        "data_asset_name": {
          "type": "string",
          "nullable": false,
          "description": "目标数据资产名称，引用 t_factory_config_asset.data_asset_name。管道不拥有资产表定义。"
        },
        "trigger": {
          "type": "json",
          "nullable": true,
          "description": "可选的执行触发器. 当前只支持 CDC 触发器"
        },
        "processing_config": {
          "type": "json",
          "nullable": false,
          "description": "源到资产的数据加工配置。内置实现期望 source.schema、source.table 和 field_mappings；目标表名、类型、schema 和主键统一从 t_factory_config_asset 解析。"
        }
      },
      "primaryKey": [
        "name"
      ]
    }
  ]
}
